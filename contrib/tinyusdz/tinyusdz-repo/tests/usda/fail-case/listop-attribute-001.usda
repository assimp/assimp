#usda 1.0
(
    metersPerUnit = 1
    upAxis = "Y"
    defaultPrim = "hello"
)

def Cone "hello"
(
)
{
  # list edit qualifier is not allowed for Attribute
  add double myheight.connect = </hello.height>
}
