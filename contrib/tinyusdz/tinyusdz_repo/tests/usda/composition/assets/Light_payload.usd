#usda 1.0
(
    defaultPrim = "Light"
    upAxis = "Z"
)

def Xform "Light" (
    assetInfo = {
        asset identifier = @assets/Light.usd@
        string name = "Light"
    }
    kind = "component"
    prepend references = @./Light.geom.usd@</Light>
)
{
}
