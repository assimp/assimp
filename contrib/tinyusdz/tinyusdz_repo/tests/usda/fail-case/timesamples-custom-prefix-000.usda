#usda 1.0

def "muda"
{
    # custom + timeSamples one liner is not valid in pxrUSD
    # and `custom` keyword will be deprecated.
    # https://github.com/PixarAnimationStudios/USD/issues/2069
    custom float spin.timeSamples = {
        0: 0,
        192: 1440,
    }
}
